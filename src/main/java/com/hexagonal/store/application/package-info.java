/**
 * Paquete que contiene los casos de uso y servicios de aplicaciu00f3n.
 * 
 * En la arquitectura hexagonal, la capa de aplicaciu00f3n contiene:
 * - Puertos de entrada (casos de uso)
 * - Puertos de salida (repositorios)
 * - Servicios de aplicaciu00f3n que implementan los casos de uso
 */
package com.hexagonal.store.application;
