/**
 * Paquete que contiene los mappers para convertir entre entidades de dominio y entidades JPA.
 * 
 * Los mappers de persistencia:
 * - Convierten entidades de dominio a entidades JPA para la persistencia
 * - Convierten entidades JPA a entidades de dominio para el uso en la aplicaciu00f3n
 * - Ayudan a mantener la separaciu00f3n entre la capa de dominio y la capa de infraestructura
 */
package com.hexagonal.store.infrastructure.output.persistence.mapper;
